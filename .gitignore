# --- our build OUTPUTS only (regenerated; not committed) ---
reference/*.bin
reference/kernel-obj/
toolchain/smoke/
build.log
build.err

# --- editor / OS cruft ---
Thumbs.db
*.user

# NOTE: vendor/ IS committed on purpose (leak source + SH toolchain) so the repo
#       clones-and-builds offline. Only C:\wcedreamcast (the DC SDK) is external
#       and fetched separately — see RESUME.md / toolchain\bootstrap.ps1.
